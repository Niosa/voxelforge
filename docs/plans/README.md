# docs/plans

Agent and design plans live here while they are active. Delete a plan when its work ships — git history is the archive.

Session logs and agent output (`agentoutput.txt`, trajectory files, `.kilo/`) are gitignored and never committed.
